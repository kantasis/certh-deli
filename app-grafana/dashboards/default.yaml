apiVersion: 1

providers:
  - name: Default # A uniquely identifiable name for the provider
    folder: ONCODIR-DELI # The folder where to place the dashboards
    type: file
    options:
      path: "${GLOBAL_GF_PATHS_PROVISIONING}/dashboards"
